# Backend server port (default: 8001). Use a different port if 8001 is in use.
PORT=8001

# Database URL. Dev: SQLite file. Prod: PostgreSQL (e.g. postgresql+psycopg2://user:pass@host/db).
DATABASE_URL=sqlite:///./simulator.db
